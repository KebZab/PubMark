# Secrets — never commit
.mcp.json
.env
.env.*
!.env.example
mcpenv.txt
server/.env
server/.env.*
!server/.env.example

# Database dumps — contain real user emails, phone numbers, password hashes
dumps/

# Installed dependencies and generated build output stay local.
node_modules/
dist/
web-build/
dist-web/
.expo/

.vercel/
