# You normally do NOT need this file for local development.
#
# In development the app works out the API address automatically: it reuses
# whatever host your phone used to reach the Expo dev server and swaps in the
# API port. So changing Wi-Fi, rebooting the router, or getting a new DHCP
# lease all just work, with nothing to edit here.
#
# This value is only used for a production build (an installed .apk/.ipa),
# where there is no dev server to ask. Point it at your deployed API.

# EXPO_PUBLIC_API_BASE_URL=https://api.your-deployed-server.com/api
