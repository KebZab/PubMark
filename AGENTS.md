# Start here — PubMark

Before working on this repository, read [AI_PROJECT_HANDOFF.md](AI_PROJECT_HANDOFF.md). It contains the current architecture, setup instructions, standalone-map decisions, transfer checklist, and known documentation conflicts.

- Current source code and the user's latest instructions take precedence over historical project documents.
- For work under `mobile/`, also follow [mobile/AGENTS.md](mobile/AGENTS.md), including its version-specific Expo documentation requirement.
- Preserve existing user changes. Do not reset the worktree or regenerate map data without understanding the impact.
- Never print or copy private environment values, `mcpenv.txt`, connector credentials, or database dumps into documentation or logs.
- The standalone local stall map is intentionally independent of the system database. Do not upload, seed, migrate, or replace its data without an explicit request.
- Setup instructions are not authorization to mutate a shared database, launch public tunnels, rotate credentials, or execute the paused security-remediation plan.
